<configuration>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="achabitation-backend-api"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} level=%level app=${APP_NAME} requestId=%X{requestId:-none} thread=%thread logger=%logger{36} msg="%replace(%msg){'\"','\\\"'}"%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
